/compose.override.yaml
/docs/build
/.megalinter-python-deps/

# npm
/bats-*.tgz
# we don't have any deps; un-ignore if that changes
/package-lock.json
test/.bats/run-logs/
# scratch file that should never be committed
/test.bats
/test2/
report.xml
report.log
